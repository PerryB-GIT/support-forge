# HeyGen Avatar Video Generator Dependencies
# Install with: pip install -r requirements-video.txt

# HTTP client with retry support
requests>=2.28.0

# For type hints backport (Python < 3.10)
typing-extensions>=4.0.0

# Optional: Progress bars for downloads
tqdm>=4.64.0

# Optional: Async support for concurrent video generation
aiohttp>=3.8.0
aiofiles>=23.0.0

# Optional: Better environment variable handling
python-dotenv>=1.0.0
