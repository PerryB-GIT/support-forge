# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
# Set to your frontend URL in production (e.g., https://support-forge.com)
# Use '*' for development to allow all origins
CORS_ORIGIN=*

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Addresses
ADMIN_EMAIL=admin@support-forge.com
FROM_EMAIL=noreply@support-forge.com

# Future configurations
# DATABASE_URL=postgresql://user:password@localhost:5432/supportforge
